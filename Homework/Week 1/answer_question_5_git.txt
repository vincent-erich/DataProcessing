5) A git branch can be seen as an independent line of development. When you want to add a new feature or fix a bug, you create a new branch. You can then add and commit changes to that branch without affecting the master branch. When you are done developing the new feature or fixing the bug, you can merge the two branches, causing the independent lines of development to be integrated into a single branch (which is, in most cases, the master branch).   